language: node_js

node_js:
- "stable"

sudo: false

jobs:
  include:
  - stage: lib
    script:
      - cd lib
      - npm install
      - npm run lint
      - npm run build
  - stage: example
    script:
      - cd example
      - npm install
      - npm run lint
      - npm run test